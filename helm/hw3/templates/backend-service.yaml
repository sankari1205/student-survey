apiVersion: v1
kind: Service
metadata:
  name: hw3-backend
spec:
  selector:
    app: hw3-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30082
  type: NodePort